import events from "events";
/**
 *
 *  APP EVENTS FOR SOCKETS
 *
 */
const ExpressEvents = new events.EventEmitter();

export const SocketEvents = {};

export const register = socket => {};

export const registerListeners = socket => {};

export default ExpressEvents;
